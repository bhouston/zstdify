{
  "name": "zstdify-monorepo",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.17.0",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "rimraf dist packages/**/dist node_modules packages/**/node_modules",
    "check": "biome check --write",
    "tsgo": "pnpm -r tsgo",
    "test": "pnpm vitest",
    "lint": "pnpm biome check",
    "make-release:zstdify": "node scripts/make-release.ts packages/zstdify",
    "make-release:cli": "node scripts/make-release.ts packages/cli"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.4",
    "@types/node": "^24",
    "@typescript/native-preview": "7.0.0-dev.20251202.1",
    "@vitest/coverage-v8": "^2.1.9",
    "rimraf": "^6.0.1",
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "type": "module"
}
