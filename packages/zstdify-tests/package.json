{
  "name": "zstdify-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest",
    "tsgo": "tsc --noEmit",
    "generate:corpus": "node scripts/generate-corpus.ts",
    "regression:compression": "node scripts/compression-regression.ts",
    "fuzz:decompress": "node scripts/fuzz-decompress.ts"
  },
  "devDependencies": {
    "@types/node": "^24",
    "fast-check": "^4.5.3",
    "typescript": "^5.6.0",
    "vitest": "^2.1.9",
    "zstdify": "workspace:*"
  }
}
